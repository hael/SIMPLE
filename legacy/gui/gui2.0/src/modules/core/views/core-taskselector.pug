div
	h="Task Selector"
	img#closebutton(
		src="img/close.png"
		onclick="taskselector.hide()"
	)

div
	p.inputlabel="Input File (*.simple)"
	div
		input#inputpath(type="text")
		img(
			src="img/folder.png"
			onclick="browser.show({buttons : [{ name : 'select', action : 'taskselector.refineTasks'}], path : '" + folder + "', filter : '.simple'})"
		)

div
	div.tasklist
		b.tasklisttitle="Package"
		each module in tasks
			div.tasklistmodules(
				onclick="taskselector.refineTasks('" + module['moduletitle'] +"')"
				data-module=module['moduletitle']
			)=module['moduletitle']
	div.tasklist
		b.tasklisttitle="Task"
		div.tasklisttasks
			each module in tasks
				each task in module['tasks']
					div.task(
						data-module=module['moduletitle']
						data-modulename=module['modulename']
						data-task=task['name']
						data-description=task['descr_short']
						onclick="taskselector.select(this)"
					)=task['name']
				button.task#showalltasks(
					data-module=module['moduletitle']
					data-modulename=module['modulename']
					onclick="taskselector.showAllTasks(this, '" + module['moduletitle'] +"')"
				)="Show All"
				
div
	p.inputlabel="Description"
	div#taskdescription

div#selectorbuttons
	button(onclick="tasksetup.show()")="Select"
