# Version Configuration file for @PROJECT_NAME@ library.
set(SIMPLE_VERSION "@SIMPLE_VERSION@")

set(SIMPLE_SOURCE_PATH  "@CMAKE_SOURCE_DIR@")
set(SIMPLE_PATH        "@CMAKE_INSTALL_PREFIX@")

if("${PACKAGE_FIND_VERSION_MAJOR}" EQUAL "@SIMPLE_VERSION_MAJOR@" )
   set(PACKAGE_VERSION_COMPATIBLE 1)
   if("${PACKAGE_FIND_VERSION_MINOR}" EQUAL @SIMPLE_VERSION_MINOR@ )
     set(PACKAGE_VERSION_EXACT 1)
   endif("${PACKAGE_FIND_VERSION_MINOR}" EQUAL @SIMPLE_VERSION_MINOR@ )
endif("${PACKAGE_FIND_VERSION_MAJOR}" EQUAL @SIMPLE_VERSION_MAJOR@ )
