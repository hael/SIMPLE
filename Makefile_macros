# Makefile_macros

####### The project path #####

Simple_source=/home/hael/src/simple3.0

####### The switches #####

DOPENMP = -fopenmp -DOPENMP 
DCUDA = 
DBENCH = 

####### The compilers paths #####

CCCOMP=gcc-5
GCC=g++-5
GFORTRAN=gfortran-5

####### The CUDA paths #####

CUDADIR=

####### Modules and objects directories. #####

OBJDIR=obj/SIMPLEOFILES
MODDIR=obj/SIMPLEOFILES

##############
# C compiler.#
##############

CC=$(CCCOMP)
CFLAGS= -O3 -O -DLINUX -fopenmp -DOPENMP   
CCLIB=$(CC) -c $(CFLAGS) -I $(MODDIR)  -I$(Simple_source)/simple_utils        \
                         -I /usr/include/

##############
# Preprocessor .#
##############

FPP=cpp-5 
FPPFLAGS= -P $(DOPENMP) -J $(MODDIR) -I $(OBJDIR) -I $(MODDIR) -I$(Simple_source)/simple_utils  \
                         -I /usr/include/

################
# C++ compiler.#
################

CPP=$(GCC)
CPPFLAGS=-DADD_ -O3 -DLINUX -fopenmp -DOPENMP   
CPPCLIB=$(CPP) -c $(CPPFLAGS) -I $(MODDIR)  -I$(Simple_source)/simple_utils        \
                              -I $(Simple_source)/test_code                           \
                              -I /usr/include/

#######################
# Fortran 90 compiler.#
#######################

# $(DLIB)
F90C=$(GFORTRAN)
F90FLAGS=-ffree-form -cpp -fPIC -fno-second-underscore -ffree-line-length-none -O3 -DLINUX -fopenmp -DOPENMP    $(FFLAGS)
F90CLIB=$(F90C) -c $(F90FLAGS) -I $(MODDIR)  -I$(Simple_source)/simple_utils  \
                             -J $(MODDIR)                      \


F90CLIB77=$(F90C) -c $(F90FLAGS77) -I .   -I$(Simple_source)/simple_utils  \
                                   -I $(MODDIR)                        \
                                   -J $(MODDIR)                        \

F90POST=

