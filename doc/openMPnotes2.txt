HOW TO DEAL WITH INCREMENTS

!$omp parallel
!$omp do linear(j:1) doi=1,N,2
j=j+1
      b(j) = a(i) * 2.0
   end do
!$omp end parallel

SECTIONS ARE TREATED A SINDEPENDENT UNITS OF WORK

PROGRAM EXAMPLE
!$OMP PARALLEL PROC_BIND(CLOSE) NUM_THREADS(4)
CALL WORK()
!$OMP END PARALLEL
END PROGRAM EXAMPLE
